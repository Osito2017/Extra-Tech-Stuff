#!
mkdir /var/mobile/payload
cd /var/mobile/payload
pause 
#wait for user input; "Where is the payload locate and what is it's name?"
#user defines location 
#script will spit out the files in that directory 
#user defines file name 
#script will perform commands on defined file 
